name: Call Timeout Processor

on:
  schedule:
    - cron: "*/1 * * * *"
  workflow_dispatch:

jobs:
  call-timeout:
    runs-on: ubuntu-latest

    steps:
      - name: Call Supabase Function
        run: |
          curl --fail-with-body -X POST \
          https://uvdwoisdcikzhqjwbhog.supabase.co/functions/v1/call-timeout-processor \
          -H "Content-Type: application/json" \
          -H "x-cron-secret: ${{ secrets.SUPABASE_CRON_SECRET }}" \
          -d '{}'
